\documentclass[
    paper=a4,
    fontsize=11pt,
    DIV=11,
    parskip=half,
    headings=normal,
    numbers=noendperiod,
    abstract=true,
    titlepage=true
]{scrartcl}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[english]{babel}
\usepackage{microtype}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{scrlayer-scrpage}
\usepackage[hidelinks]{hyperref}

\renewcommand{\arraystretch}{1.22}

\hypersetup{
    pdftitle={Independent Component Analysis: Foundations, Applications, and Audio Demonstration},
    pdfauthor={Hamza LOUKILI}
}

\clearpairofpagestyles
\automark{section}
\ihead{\headmark}
\ohead{Independent Component Analysis}
\cfoot{\pagemark}
\KOMAoptions{headsepline=true}
\setkomafont{pageheadfoot}{\normalfont\small}
\setkomafont{disposition}{\normalfont\bfseries}

\titlehead{\centering Technical Project Report}
\subject{Signal Processing and Blind Source Separation}
\title{Independent Component Analysis}
\subtitle{Foundations, algorithms, applications, and a reproducible audio demonstration}
\author{Hamza LOUKILI}
\date{August 2026}
\publishers{\href{https://github.com/loukili27/ICA-Voices}{github.com/loukili27/ICA-Voices}}

\begin{document}
\hypersetup{pageanchor=false}
\maketitle
\hypersetup{pageanchor=true}

\pagenumbering{roman}
\begin{abstract}
Signals encountered in audio, neuroscience, biomedical sensing, telecommunications, and finance are often mixtures of several latent phenomena. Independent Component Analysis (ICA) is a statistical framework for estimating latent components that are as independent as possible from multivariate observations. This report presents the linear ICA model, its identifiability assumptions, the principal estimation criteria, the FastICA algorithm, and representative application domains. A reproducible audio experiment then applies FastICA to three mono mixtures containing 264,515 samples each at 44.1 kHz. The algorithm converges in four iterations and reconstructs the observations with a relative root mean square error of $4.219\times10^{-16}$. Because isolated reference sources are unavailable, these measurements establish numerical consistency rather than perfect source recovery. A brief final section positions ICA relative to Principal Component Analysis (PCA) without making that comparison the central subject of the report.
\end{abstract}

\tableofcontents
\listoffigures
\newpage
\pagenumbering{arabic}

\section{Introduction}

Signals are ubiquitous and frequently overlap. Voices recorded in a room, readings collected by multiple sensors, or electrical potentials measured on the scalp can each contain contributions from several underlying sources. Separating those sources is essential when the mixtures obscure the information of interest.

\emph{Blind source separation} seeks to recover latent signals while knowing little or nothing about the sources or the physical mixing process. Independent Component Analysis (ICA) provides a principled solution when the observations can be modeled as linear mixtures of statistically independent, mainly non-Gaussian sources \cite{comon1994,hyvarinen2000}. Instead of relying only on covariance, ICA exploits higher-order and information-theoretic structure.

ICA is not a single-purpose audio technique. It is a general latent-variable method whose usefulness depends on the physical model, the statistical independence assumption, source non-Gaussianity, and the availability of sufficiently distinct observations.

This report develops the ICA model and its assumptions, derives the main estimation criteria, explains FastICA, reviews representative applications, and presents a fully reproducible audio demonstration. Only after the ICA analysis is complete is PCA mentioned briefly to clarify when each method is appropriate.

\section{Theoretical foundation of ICA}

\subsection{Linear instantaneous mixing model}

Let the unknown source vector be
\begin{equation}
    \mathbf{s}(t)=[s_1(t),s_2(t),\ldots,s_n(t)]^{\mathsf T},
\end{equation}
and let the observed mixtures be
\begin{equation}
    \mathbf{x}(t)=[x_1(t),x_2(t),\ldots,x_m(t)]^{\mathsf T}.
\end{equation}
The basic ICA model assumes an instantaneous linear mixture
\begin{equation}
    \mathbf{x}(t)=\mathbf{A}\mathbf{s}(t),
    \label{eq:mixing}
\end{equation}
where $\mathbf{A}\in\mathbb{R}^{m\times n}$ is an unknown mixing matrix. Each coefficient $a_{ij}$ represents the contribution of source $j$ to observation $i$.

In the determined setting, $m=n$ and $\mathbf{A}$ is invertible. ICA estimates an unmixing matrix $\mathbf{W}$ so that
\begin{equation}
    \mathbf{y}(t)=\mathbf{W}\mathbf{x}(t)
    =\mathbf{W}\mathbf{A}\mathbf{s}(t)\approx\mathbf{s}(t).
    \label{eq:unmixing}
\end{equation}

\begin{figure}[htbp]
    \centering
    \fbox{\begin{minipage}{0.88\textwidth}
        \centering
        \small
        \vspace{0.7em}
        \begin{tabular}{ccccc}
            $\boxed{\mathbf{s}(t)}$ & $\xrightarrow{\ \mathbf{A}\ }$ &
            $\boxed{\mathbf{x}(t)}$ & $\xrightarrow{\ \mathbf{W}\ }$ &
            $\boxed{\mathbf{y}(t)\approx\mathbf{s}(t)}$ \\[0.6em]
            independent sources & & observed mixtures & & estimated components
        \end{tabular}
        \vspace{0.7em}
    \end{minipage}}
    \caption{Linear instantaneous ICA model. The unknown matrix $\mathbf{A}$ mixes the latent sources, while ICA estimates an unmixing matrix $\mathbf{W}$ from the observations.}
    \label{fig:ica-model}
\end{figure}

Figure~\ref{fig:ica-intuitive} retains the intuitive illustration from the
original report: several physical sources contribute simultaneously to each
microphone signal, and ICA attempts to recover one latent component per
output. It complements the compact matrix representation in
Figure~\ref{fig:ica-model}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.62\textwidth]{figures/ica_separation.png}
    \caption[Intuitive blind source separation example retained from the
    original report.]{Intuitive blind source separation example retained from the
    original project materials. The source signals $\mathbf{s}(t)$ are mixed
    into microphone observations $\mathbf{x}(t)=\mathbf{A}\mathbf{s}(t)$,
    before ICA estimates $\mathbf{y}(t)=\mathbf{W}\mathbf{x}(t)$. The original
    external source of this legacy illustration should be confirmed before
    public redistribution.}
    \label{fig:ica-intuitive}
\end{figure}

\subsection{Statistical independence}

The central assumption is that the sources are mutually independent. For all admissible values,
\begin{equation}
    p_{s_1,\ldots,s_n}(s_1,\ldots,s_n)
    =\prod_{i=1}^{n}p_{s_i}(s_i).
    \label{eq:independence}
\end{equation}
Independence implies zero covariance when moments exist, but the converse is generally false. Two variables can be uncorrelated while remaining dependent through nonlinear or higher-order relationships. This is precisely why diagonalizing a covariance matrix with PCA does not, in general, solve the ICA problem.

\subsection{Non-Gaussianity and identifiability}

The classical linear ICA model permits at most one Gaussian source. A multivariate Gaussian distribution that has been whitened is rotationally symmetric: any orthogonal rotation produces the same distribution. Therefore covariance and Gaussian likelihood alone cannot identify a unique rotation. Non-Gaussian source distributions break this rotational ambiguity and make the independent directions identifiable, apart from unavoidable permutation and scaling ambiguities \cite{hyvarinenbook}.

The central-limit intuition is that a normalized sum of independent random variables tends to be more Gaussian than the original variables. ICA algorithms can therefore search for projections that are maximally non-Gaussian.

\subsection{Intrinsic ambiguities}

Even under ideal assumptions, ICA cannot determine the original component order because
\begin{equation}
    \mathbf{A}\mathbf{s}=(\mathbf{A}\mathbf{P}^{-1})(\mathbf{P}\mathbf{s})
\end{equation}
for any permutation matrix $\mathbf{P}$. It also cannot determine component scale because a source may be multiplied by a nonzero scalar if the corresponding column of $\mathbf{A}$ is divided by the same scalar. Consequently, sign changes, scale changes, and component permutations do not indicate failure.

\section{Mathematical criteria and estimation}

\subsection{Centering and whitening}

ICA is usually applied after centering and whitening. If $\boldsymbol{\mu}=\mathbb{E}[\mathbf{x}]$, centering gives
\begin{equation}
    \mathbf{x}_c=\mathbf{x}-\boldsymbol{\mu}.
\end{equation}
Let the covariance eigendecomposition be
\begin{equation}
    \mathbf{C}_x=\mathbb{E}[\mathbf{x}_c\mathbf{x}_c^{\mathsf T}]
    =\mathbf{E}\mathbf{D}\mathbf{E}^{\mathsf T}.
\end{equation}
A common whitening transform based on this eigendecomposition is
\begin{equation}
    \mathbf{z}=\mathbf{D}^{-1/2}\mathbf{E}^{\mathsf T}\mathbf{x}_c,
    \qquad
    \mathbb{E}[\mathbf{z}\mathbf{z}^{\mathsf T}]=\mathbf{I}.
    \label{eq:whitening}
\end{equation}
Whitening removes second-order correlations and constrains the remaining ICA mixing transform to a rotation. It simplifies the optimization but does not by itself establish independence.

\subsection{Kurtosis}

For a zero-mean variable $y$, the fourth-order cumulant is
\begin{equation}
    \operatorname{kurt}(y)=\mathbb{E}[y^4]-3\bigl(\mathbb{E}[y^2]\bigr)^2.
    \label{eq:kurtosis}
\end{equation}
A Gaussian variable has zero excess kurtosis. Positive and negative values characterize super-Gaussian and sub-Gaussian behavior, respectively. Maximizing the absolute kurtosis of standardized projections is one possible ICA contrast, although kurtosis can be sensitive to outliers.

\subsection{Negentropy}

Differential entropy is
\begin{equation}
    H(y)=-\int p_y(u)\log p_y(u)\,du.
\end{equation}
Among variables with equal variance, the Gaussian distribution has maximum entropy. Negentropy therefore defines a nonnegative measure of non-Gaussianity:
\begin{equation}
    J(y)=H(y_{\mathrm{Gauss}})-H(y)\geq0.
    \label{eq:negentropy}
\end{equation}
Because exact entropy estimation is difficult, FastICA uses robust approximations based on smooth nonlinear functions such as $G(u)=\log\cosh(u)$ \cite{hyvarinen1999}.

\subsection{Mutual information}

Mutual information measures dependence between the components of $\mathbf{y}$:
\begin{equation}
    I(y_1,\ldots,y_n)
    =\sum_{i=1}^{n}H(y_i)-H(\mathbf{y})\geq0.
    \label{eq:mutual-info}
\end{equation}
It equals zero if and only if the components are mutually independent under standard regularity conditions. For whitened data, minimizing mutual information is closely connected to maximizing component non-Gaussianity \cite{comon1994}.

\subsection{Maximum likelihood}

If $\mathbf{y}=\mathbf{W}\mathbf{x}$ and the source densities $p_i$ are modeled, the change-of-variables formula gives
\begin{equation}
    p_{\mathbf{x}}(\mathbf{x};\mathbf{W})
    =|\det\mathbf{W}|\prod_{i=1}^{n}p_i(\mathbf{w}_i^{\mathsf T}\mathbf{x}).
\end{equation}
For $T$ samples, the log-likelihood is
\begin{equation}
    \ell(\mathbf{W})
    =T\log|\det\mathbf{W}|
    +\sum_{t=1}^{T}\sum_{i=1}^{n}
    \log p_i(\mathbf{w}_i^{\mathsf T}\mathbf{x}(t)).
    \label{eq:likelihood}
\end{equation}
Maximizing this expression encourages an invertible transform whose outputs follow factorized source models.

\subsection{FastICA fixed-point update}

For whitened samples $\mathbf{z}$ and a unit vector $\mathbf{w}$, one FastICA update has the form
\begin{equation}
    \mathbf{w}^{+}
    =\mathbb{E}\!\left[\mathbf{z}\,g(\mathbf{w}^{\mathsf T}\mathbf{z})\right]
    -\mathbb{E}\!\left[g'(\mathbf{w}^{\mathsf T}\mathbf{z})\right]\mathbf{w},
    \qquad
    \mathbf{w}\leftarrow\frac{\mathbf{w}^{+}}{\|\mathbf{w}^{+}\|_2}.
    \label{eq:fastica}
\end{equation}
The parallel algorithm updates all rows of $\mathbf{W}$ and then applies symmetric decorrelation,
\begin{equation}
    \mathbf{W}\leftarrow
    (\mathbf{W}\mathbf{W}^{\mathsf T})^{-1/2}\mathbf{W},
\end{equation}
until the directions stabilize. The fixed-point approach is fast and avoids manually selecting a learning rate \cite{hyvarinen1999}.

\section{Applications and use cases}

ICA is most valuable when several recorded variables contain overlapping contributions from latent processes and those processes have a plausible interpretation. Its role varies across domains:
\begin{itemize}
    \item \textbf{Audio and acoustics:} separating simultaneously recorded voices or instruments, enhancing speech, analyzing microphone arrays, and studying the cocktail-party problem.
    \item \textbf{Neuroscience:} removing eye-blink, muscle, cardiac, and line-noise artifacts from EEG or MEG; ICA can also provide an exploratory decomposition of brain activity \cite{makeig1996}.
    \item \textbf{Biomedical sensing:} isolating physiological components and measurement artifacts in ECG, imaging, and multimodal sensor recordings.
    \item \textbf{Images and spatial data:} decomposing image collections into latent spatial patterns and extracting statistically distinct features.
    \item \textbf{Telecommunications:} blind channel identification, multi-user detection, and source separation in multi-antenna systems.
    \item \textbf{Industrial monitoring:} separating process variations, disturbances, and sensor effects for exploratory fault detection.
    \item \textbf{Finance:} exploring statistically independent factors in correlated returns, subject to strong cautions about nonstationarity and economic interpretation.
\end{itemize}

These examples share a common objective: recovering interpretable latent mechanisms rather than merely compressing the observations. ICA is therefore especially relevant when the components themselves matter.

The assumptions must always be checked against the application. Reverberant audio, propagation delays, nonlinear sensors, nonstationary sources, and time-varying mixtures require extensions beyond instantaneous linear ICA. When the latent sources are not plausibly independent, an ICA decomposition may still be mathematically computable but scientifically misleading.

\section{Reproducible audio demonstration}

\subsection{Input data and preprocessing}

The repository contains three precomputed mono WAV mixtures: \texttt{ICA mix 1.wav}, \texttt{ICA mix 2.wav}, and \texttt{ICA mix 3.wav}. Each is a 16-bit PCM signal sampled at 44,100 Hz. The shortest common segment contains 264,515 samples, corresponding to 5.998 seconds. The $264{,}515\times3$ observation matrix has numerical rank three.

The notebook converts integer samples to floating point values, aligns all observations to the same length, and centers each channel. Three genuinely distinct observations are essential: duplicating one recording would produce a rank-deficient matrix and would not create enough information to recover three components.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/observed_mixtures.png}
    \caption{Observed audio mixtures used by the demonstration: three mono signals sampled at 44.1 kHz over 5.998 seconds.}
    \label{fig:observed-mixtures}
\end{figure}

\subsection{FastICA configuration}

The implementation uses scikit-learn's \texttt{FastICA} estimator with the parameters in Table~\ref{tab:fastica-config} \cite{sklearnfastica}. The random state is fixed for reproducibility. Unit-variance whitening is specified explicitly, and the \texttt{logcosh} contrast provides a smooth negentropy approximation.

\begin{table}[htbp]
    \centering
    \caption{FastICA configuration used in the notebook.}
    \label{tab:fastica-config}
    \begin{tabular}{ll}
        \toprule
        Parameter & Value \\
        \midrule
        Number of components & 3 \\
        Algorithm & Parallel \\
        Whitening & Unit variance \\
        Nonlinearity & \texttt{logcosh} \\
        Maximum iterations & 2,000 \\
        Tolerance & $10^{-5}$ \\
        Random state & 42 \\
        \bottomrule
    \end{tabular}
\end{table}

After estimation, each component is normalized independently to 95\% of the signed 16-bit range before WAV export. This prevents clipping and integer overflow; it replaces the arbitrary volume multiplier used in the historical notebook.

\section{Experimental results}

\subsection{Dependence in the observations}

The observed mixtures are strongly correlated:
\begin{equation}
\mathbf{R}_{X}=\begin{bmatrix}
1.000 & 0.846 & 0.784\\
0.846 & 1.000 & 0.972\\
0.784 & 0.972 & 1.000
\end{bmatrix}.
\end{equation}
The large off-diagonal values show that the observations share substantial linear structure.

\subsection{Estimated independent components}

FastICA converged in four iterations. The estimated components have an identity sample correlation matrix to the displayed precision. Their Fisher kurtosis values are
\begin{equation}
    [1.401,\,-1.499,\,2.775],
\end{equation}
so all three components depart from Gaussianity at the fourth-order level. The signs indicate different distribution shapes: positive excess kurtosis is associated with heavier tails, while negative excess kurtosis indicates a flatter distribution. These values support the non-Gaussianity used by FastICA, but they are not by themselves proof that the recovered components match the unknown physical sources.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/estimated_ica_components.png}
    \caption{Three components estimated by FastICA. Their order, sign, and amplitude are not directly comparable with unknown source labels because of ICA's intrinsic ambiguities.}
    \label{fig:ica-components}
\end{figure}

\subsection{Numerical validation}

\begin{table}[htbp]
    \centering
    \caption{Measured outputs of the reproducible experiment.}
    \label{tab:results}
    \begin{tabular}{ll}
        \toprule
        Metric & Result \\
        \midrule
        FastICA iterations & 4 \\
        Largest absolute off-diagonal ICA correlation & $<10^{-4}$ \\
        Reconstruction RMSE & $1.608\times10^{-16}$ \\
        Relative reconstruction RMSE & $4.219\times10^{-16}$ \\
        Exported separated WAV files & 3 \\
        \bottomrule
    \end{tabular}
\end{table}

The near-machine-precision reconstruction error confirms that the estimated mixing and unmixing transforms consistently factorize the same observation matrix. It is not a source-separation score: inverse transformation reconstructs the mixtures used for fitting.

The exported WAV files are stored in \texttt{outputs/separated/} and can be played inside the notebook. Listening provides a qualitative check, but a rigorous SDR or SI-SDR evaluation would require the isolated reference sources.

\section{Limitations and responsible interpretation}

The experiment supports numerical convergence, decorrelation, non-Gaussian component estimation, reconstruction consistency, and qualitative listening. It does not support a claim of objectively perfect recovery because:
\begin{itemize}
    \item the isolated reference sources and original mixing matrix are unavailable;
    \item zero correlation is weaker than statistical independence;
    \item the instantaneous model does not explicitly represent echo, propagation delay, nonlinear mixing, or a time-varying room;
    \item the method requires sufficiently distinct observations and a full-rank mixture;
    \item multiple Gaussian sources are not identifiable without additional structure.
\end{itemize}

These qualifications correct the stronger wording in the first report version while preserving its central theoretical motivation.

\clearpage
\section{Practical positioning relative to PCA}

PCA is mentioned here only to prevent a common methodological confusion. PCA and ICA are both linear transformations, but they are designed for different goals \cite{jolliffe2002}. PCA is primarily a representation method: it orders orthogonal directions by explained variance. ICA is primarily a source-separation method: it searches for latent directions that are as statistically independent as possible.

\begin{table}[htbp]
    \centering
    \small
    \caption{Practical distinction between PCA and ICA.}
    \label{tab:pca-ica}
    \begin{tabular}{>{\raggedright\arraybackslash}p{0.24\textwidth} >{\raggedright\arraybackslash}p{0.33\textwidth} >{\raggedright\arraybackslash}p{0.33\textwidth}}
        \toprule
        Question & PCA & ICA \\
        \midrule
        Main objective & Preserve variance in fewer orthogonal variables & Recover statistically independent latent components \\
        Main information & Covariance and second-order structure & Non-Gaussianity and higher-order structure \\
        Choose it for & Dimension reduction, visualization, compression, denoising & Blind source separation, artifact removal, latent-process discovery \\
        Component order & Ranked by explained variance & No intrinsic ranking \\
        Main caution & High variance is not necessarily meaningful & Independence and mixing assumptions must be plausible \\
        \bottomrule
    \end{tabular}
\end{table}

Use PCA when the objective is to obtain a compact representation of high-dimensional data. Use ICA when the observations are believed to be mixtures and the latent signals themselves are the target. The methods can also be complementary: PCA may reduce dimension and whiten the data before ICA estimates an independent rotation.

\section{Conclusion}

ICA is a fundamental method for discovering statistically independent structure in multivariate data. Its usefulness extends from the cocktail-party problem to neuroscience, biomedical sensing, imaging, telecommunications, industrial monitoring, and exploratory factor analysis. The method relies on a suitable mixture model, source independence, non-Gaussianity, and sufficiently informative observations.

The audio experiment makes the complete workflow concrete: align and center the mixtures, whiten the data, estimate the unmixing transform with FastICA, export the components, and validate the numerical factorization. The results establish convergence and internal consistency while the absence of isolated references prevents a claim of objectively perfect separation.

The complete reproducible implementation, input mixtures, and separated outputs are available at
\begin{center}
    \href{https://github.com/loukili27/ICA-Voices}{\textbf{github.com/loukili27/ICA-Voices}}.
\end{center}

\begin{thebibliography}{9}

\bibitem{comon1994}
P. Comon, ``Independent component analysis, a new concept?'' \emph{Signal Processing}, vol. 36, no. 3, pp. 287-314, 1994. DOI: \href{https://doi.org/10.1016/0165-1684(94)90029-9}{10.1016/0165-1684(94)90029-9}.

\bibitem{hyvarinen1999}
A. Hyv\"arinen, ``Fast and robust fixed-point algorithms for independent component analysis,'' \emph{IEEE Transactions on Neural Networks}, vol. 10, no. 3, pp. 626-634, 1999. DOI: \href{https://doi.org/10.1109/72.761722}{10.1109/72.761722}.

\bibitem{hyvarinen2000}
A. Hyv\"arinen and E. Oja, ``Independent component analysis: algorithms and applications,'' \emph{Neural Networks}, vol. 13, no. 4-5, pp. 411-430, 2000. DOI: \href{https://doi.org/10.1016/S0893-6080(00)00026-5}{10.1016/S0893-6080(00)00026-5}.

\bibitem{hyvarinenbook}
A. Hyv\"arinen, J. Karhunen, and E. Oja, \emph{Independent Component Analysis}. New York: Wiley, 2001.

\bibitem{jolliffe2002}
I. T. Jolliffe, \emph{Principal Component Analysis}, 2nd ed. New York: Springer, 2002.

\bibitem{makeig1996}
S. Makeig, A. J. Bell, T.-P. Jung, and T. J. Sejnowski, ``Independent component analysis of electroencephalographic data,'' in \emph{Advances in Neural Information Processing Systems 8}, 1996, pp. 145-151.

\bibitem{sklearnfastica}
Scikit-learn developers, ``FastICA API documentation,'' \href{https://scikit-learn.org/stable/modules/generated/sklearn.decomposition.FastICA.html}{scikit-learn.org}, accessed August 24, 2026.

\end{thebibliography}

\end{document}
